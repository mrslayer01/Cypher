<form class="actor-sheet" autocomplete="off">
  <header class="sheet-header">
    {{> header}}
  </header>

  <section class="sheet-body">
    <div class="stat-block">
      <label>
        Description
      </label>
      <textarea name="system.bio.description">
        {{system.bio.description}}
      </textarea>
    </div>
  </section>
</form>